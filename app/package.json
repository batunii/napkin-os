{
  "name": "napkin-studio-os",
  "private": true,
  "version": "1.2.1",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "tauri": "tauri",
    "lint": "eslint .",
    "preview": "vite preview",
    "wasm": "cargo build -q -p napkin-wasm --target wasm32-unknown-unknown --release && wasm-bindgen --target web --typescript --out-dir src/wasm ../target/wasm32-unknown-unknown/release/napkin_wasm.wasm",
    "build:static": "npm run apps && npm run wasm && tsc -b && VITE_NAPKIN_HOST=wasm vite build",
    "apps": "cargo run -q -p clan-sdk --example make_brief_maker && mkdir -p public/apps && mv brief-maker.app.clan public/apps/"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "esbuild": "^0.28.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tauri-apps/cli": "^2.11.3",
    "@types/node": "^24.12.3",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^10.3.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.6.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.59.2",
    "vite": "^8.0.12"
  }
}
