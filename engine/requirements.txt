# Supported install: pip install -e .        (see pyproject.toml — flat, editable layout)
# Extras:            pip install -e ".[all]"  (PyYAML, python-dotenv, PyMuPDF, anthropic)
# This file mirrors the CORE dependencies for tooling that reads requirements.txt.
pdfplumber>=0.11.0      # .pdf briefs
python-docx>=1.1.0      # .docx briefs
